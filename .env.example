# Tendo — environment template
# Copy this file to `.env.local` on your machine and fill in real values.
# NEVER commit `.env.local`. NEVER paste real values in chat.

# === Phase 1 ===
# (nothing required yet — v0 has no auth, no DB, no AI calls)

# === Phase 2 (content generation pipeline) ===
# Free-tier AI providers. Sign up via the manual tasks in MANUAL_TASKS.md.
# GROQ_API_KEY=
# GEMINI_API_KEY=

# === Phase 4 (school pilot) ===
# When we add per-school namespacing, we may introduce:
# SCHOOL_SLUG_DEFAULT=demo
# ANALYTICS_KEY=
